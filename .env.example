# Backend Environment Variables
NODE_ENV=development
PORT=5000
MONGODB_URI=<YOUR_MONGODB_ATLAS_URI> # Create a MongoDB Atlas cluster and get the connection string
JWT_SECRET=<GENERATE_ANY_RANDOM_STRING> # Generate a random secret for JWT signing

# Note: You need to create a MongoDB Atlas database manually and replace the MONGODB_URI placeholder above.
